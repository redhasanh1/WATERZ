TensorRT Progress (detailed)

- Step 1 — YOLO engine: Complete
  • Engines: runs/detect/new_sora_watermark/weights/best.engine, web/best.engine
  • Loader: yolo_detector.py prefers TensorRT when available
  • INT8: Pending (needs calibration/QDQ ONNX)

- Step 2 — RAFT (optical flow) engine: Complete + Wired
  • Export: FastFlowNet ONNX (opset 16) at faster-propainter-main/engines/raft/fastflownet.onnx
  • Engine: FP16 at faster-propainter-main/engines/raft/raft_fp16.engine
  • Runtime wiring: faster-propainter-main/watermark.py uses a RAFT adapter that
    loads the TensorRT engine when available, else falls back to PyTorch RAFT.
    - No PyCUDA required: uses TensorRT Python + torch device pointers
    - Windows: ensure TensorRT lib path on PATH (TensorRT-10.7.0.23/lib)
  • Benchmarks (RTX 5070):
    - Engines from GTX 1660 Ti removed (architecture incompatible)
    - Using PyTorch .pt models until RTX 5070 engines are rebuilt
    - Expected performance after rebuild: 2-3x faster than 1660 Ti benchmarks
    - RAFT/RFCNet/ProPainter engines will need rebuild for RTX 5070

- Step 3 — RFCNet (flow completion) engine: In Progress
  • Exporter added: faster-propainter-main/tools/export_rfcnet_onnx.py
  • Blocker: torchvision.ops.deform_conv2d → requires DCNv2 TensorRT plugin
  • Interim: Optional Torch‑TensorRT segmentation (enable RFCNET_TORCHTRT=1)

- Step 4 — ProPainter engine: Pending (same DCNv2/plugin concerns apply)

- Plugins / Tooling
  • DCNv2 TRT plugin: Not built (needs VS Build Tools + CUDA VS integration)
  • Helper scripts: BUILD_RFCNET_ONNX.bat, BUILD_RFCNET_ENGINE_FP16.bat, build_dcnv2_plugin.bat
  • TRT env: SETUP_TRT_ENV.bat sets PATH for TRT DLLs

- Next Actions
  1) Build DCNv2 plugin DLL and place path into DCNV2_PLUGIN_DLL
  2) Build RFCNet FP16 engine with the plugin
  3) Optionally wire RFCNet engine with fallback similar to RAFT
